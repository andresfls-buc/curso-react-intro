import React from "react";
import { GiBroadsword } from "react-icons/gi";

function CompleteIcon({ className, onClick }) {
  return <GiBroadsword className={className} onClick={onClick} />;
}

export { CompleteIcon };
